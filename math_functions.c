#include <stdio.h>
#include <math.h>
#include <stdlib.h>

int main()
{
    //     int x = 9;
    //     x = sqrt(x);
    //     printf("The Square of x is: %d\n", x);

    //     int y = 2;
    //     y = pow(y, 3);
    //     printf("The power of y is: %d\n", y);

    float x = 45;

    // x = round(x);
    //  x = ceil(x);
    // x = floor(x);
    // x = abs(x);
    //  x = log(x);
    x = sin(x);
    printf("%f\n", x);
    return 0;
}
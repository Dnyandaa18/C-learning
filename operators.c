#include <stdio.h>

int main()
{
     int x = 20;
     int y = 3;
     int z = 0;

     // z = x + y;
     // z = x - y;
     // z = x * y;
     // z = x / y;
     // z = x % y; //Gives the remainder

    // printf("%d", z);


    // x++;
    // x--;

    //Arguemnt operators
    // x += 2; //which is the best alternative of x=x+2

    // x -= 2;
    // x*=3;
    // x /=2;
    printf("%d", x);

    return 0;
}